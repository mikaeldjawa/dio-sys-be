{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "apps/server/dist/lambda.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/server/dist/lambda.mjs"
    }
  ]
}
